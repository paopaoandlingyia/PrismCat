# === 编译产物（仅根目录）===
/prismcat
/prismcat.exe
/prismcat.exe~
/prismcat.exe.lnk

# === 运行时数据 ===
/data/
/config.yaml

# === 前端构建产物 & 嵌入到后端的 UI 文件 ===
web/dist/
web/node_modules/
# Keep a tracked placeholder so `//go:embed all:ui` still compiles on a clean clone.
# The real UI build output is copied here during build/release.
internal/server/ui/*
!internal/server/ui/.keep

# === 本地 Remotion 宣传视频实验 ===
promo-video/

# === 系统 / 编辑器文件 ===
.DS_Store
Thumbs.db
*.swp
*.swo

# === IDE ===
.vscode/
.idea/

# === 其他 ===
.gemini/
.agent/
docs/
.cache/
.gocache/
.gopath/
.gotelemetry/
.claude/
